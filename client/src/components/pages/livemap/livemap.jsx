import React from 'react';

// Load component styles
require('./livemap.scss');

export default class LiveMapPage extends React.Component {
  render() {
    return (
      <div className="live-map-page">Live map page</div>
    );
  }
}